<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             xmlns:vm="using:Auto_Auction.ViewModels"
             x:Class="Auto_Auction.Controls.CreateUserView"
             x:DataType="vm:CreateUserViewModel">
             
    
    <StackPanel>
              <Grid>
                  <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="220">
            
                      <TextBlock  Text="Create user" FontSize="28" FontWeight="Regular" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            
                      <TextBox Text="{Binding Name}" x:Name="UsernameBox" Watermark="Username" Height="40" Margin="0,0,0,12" Padding="10,0"/>
            
                      <TextBox x:Name="PasswordBox" Text="{Binding Password}" Watermark="Password" Height="40" Margin="0,0,0,12" Padding="10,0" PasswordChar="•"/>
                      
                      <TextBox Text="{Binding Passwordagain}" x:Name="PasswordagainBox" Watermark="Password again" Height="40" Margin="0,0,0,16" Padding="10,0" PasswordChar="•"/>
            
                      <TextBox Text="{Binding CvrNumber}" x:Name="VCRBox" Watermark="VCR" Height="40" Margin="0,0,0,16" Padding="10,0" ></TextBox>
                      <TextBox Text="{Binding Credit}" x:Name="CreditBox" Watermark="Credit" Height="40" Margin="0,0,0,16" Padding="10,0" IsVisible="{Binding IsChecked, ElementName=CorporateButton}"></TextBox>
                      
                      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                          <RadioButton Name="CorporateButton" Content="Corporate" Margin="0,0,20,0" IsChecked="True"/>
                          <RadioButton Content="Private"/>
                      </StackPanel>
                      
                      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
                          <Button Content="Cancel" Width="100" Height="40" Background="White" Foreground="Gray" BorderBrush="Gray" Click="Cancel"/>
                          <Button Content="Create" Width="100" Height="40" Background="Green" Foreground="White" Click="RegisterUser"/>
                      </StackPanel>
                  </StackPanel>
              </Grid>
          </StackPanel>
</UserControl>
